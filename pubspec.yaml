name: halflife_wiki
description: "A new Flutter project."
publish_to: 'none'  # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.7.0 <4.0.0"  # Certifique-se de especificar a versão do SDK corretamente

dependencies:
  flutter:
    sdk: flutter
  loading_animation_widget: ^1.3.0
  cupertino_icons: ^1.0.8
  carousel_slider: ^5.0.0
  shared_preferences: ^2.0.11
  http: ^1.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true 
  assets:
    - assets/images/
  fonts:
    - family: HalfLife
      fonts:
        - asset: assets/fonts/halflife2.ttf

